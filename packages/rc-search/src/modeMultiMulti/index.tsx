import React from "react";
import { IRcSearchProps } from "../types/IRcSearchProps.type";
const ModeSingleSingle: React.FC<IRcSearchProps> = (props) => {
    return (
        <div>mode 3 : multi multi</div>
    )
}

export default ModeSingleSingle;