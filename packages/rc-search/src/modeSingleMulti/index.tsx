import React from "react";
import { IRcSearchProps } from "../types/IRcSearchProps.type";
const ModeSingleMulti: React.FC<IRcSearchProps> = (props) => {
    return (
        <div>mode 2 : single multi</div>
    )
}

export default ModeSingleMulti;