import styled from 'styled-components';

export const SSelectionWrapper = styled.div`
  > * {
    display: inline-block;
    margin-right: 8px;
  }
`;

export const SSelectAllContainer = styled.span`
  display: inline-block;
  padding: 0 17px;
  margin-right: 0;
`;
