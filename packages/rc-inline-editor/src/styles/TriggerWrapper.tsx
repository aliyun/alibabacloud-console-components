import styled from 'styled-components';

export const TriggerWrapper = styled.span `
  visibility: hidden;
  display: inline;
  outline: none;
  > div {
    display: inline;
    line-height: 16px;
    border: none;
  }
  cursor: pointer;
`;
