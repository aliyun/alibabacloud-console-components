import styled from 'styled-components';

export const LabelWrapper = styled.div `
  &:hover > span {
    visibility: visible;
  }
  .editor-text-trigger {
    visibility: visible;
    outline: none;
  }
`;
